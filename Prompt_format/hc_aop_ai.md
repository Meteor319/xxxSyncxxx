
#VibeConsult
我是IC設計產業的營運規劃辦公室的技術主管, 我需要預估每一個季度在不同SOC專案所需要投入的人力, 
- 因此我的基本計算概念是, 如果是project_a 今年進入sample back階段, 可以參考project_b sample back之後的人數, 並依照quarter計算
- 我想用markdown的格式整理我工作上的經驗, 那是一個需要基於現在資料套用過去計算方式的模板, 未來會需要用新的資料, 套用模板進行新的計算, 

對於這樣的任務, 你身為GPT與LLM專家, 能給我什麼建議嗎



我覺得這樣的模板不錯, 但希望能夠在不失去精準度的前提下, 幫我簡化, 畢竟我手上的project可能有100個


但我的計算方式可能比較簡單一些, 主要流程是

1.我會先定義用今年度(Y2025, Q1~Q4) 的資料, 預估明年(Y2026 Q1~Q4) 各SOC專案的投入人力
2.每個專案我會跟你說之前主管預估的經驗是什麼, 例如: 是參考哪一個專案的某個階段的人力, 再進行少部分的計算
3.可能會考慮Y2026是否有什麼重要的project milestone, 會另外微調計算結果
4.以上的流程會需要請LLM model計算並說明原因

請基於以上流程, 給我於template的設計


我覺得你有考慮到project scope跟condition的調整, 這是一個很棒的元素, 請將以上討論的內容, 整理成可執行且易於維運的markdown template


如果過程中會需要請LLM查詢excel, 找出專案名稱與每季度的人力, project milestone, 這樣我該如何設計LLM中的prompt與excel format


但如果將所有資訊參考資料都寫在prompt中, 可能會提早讓token用完

## 需要定義的欄位:
- Project_id, Ref_Project_id
- Base_Year, Target_Year



我目前有以下幾個檔案, 我會給你檔案內容與使用說明, 請再告訴我, 要如何使用LLM達成我要的年度預估Project quarterly headcount的目的:

#data source

-1.過去SOC 的專案名稱, project milestone, 每一季的投入人力

-2.主管預估的邏輯, 舉例來說會包含, 這個project_a 是要參考project_b, 並且會紀錄是基於什麼原因, 以及是否需要另外進行小部分計算, 例如乘上1.1 或是乘上0.8等等; 也有可能是參考彼此的pre-silicon或post-silicon階段, 進行比較或參考

-3.明年的project清單與時程規劃, 這像是明年需要投入的人力, 會需要結合1 & 2的內容, 進行預估
-

################################################################################################################################################


**情境：**
請根據提供的計算模板，將新的資料套用模板中的核心邏輯進行計算。

**輸入 A：計算模板 (請將完整的 Markdown 模板貼在這裡)**

**輸入 B：新的輸入資料 (請將新的資料以表格/JSON/CSV格式貼在這裡)**

**任務：**
1.  **步驟確認：** 簡要列出模板中的**核心計算邏輯**中的所有步驟。
2.  **執行計算：** 根據步驟，對「輸入 B」中的資料進行計算。
3.  **輸出結果：** 以清晰的表格或 JSON 格式，只輸出**最終的計算結果**和**所有重要的中間計算結果** (如果模板有定義的話)。
4.  **範例對照：** 計算完成後，請檢查結果是否符合模板中**過去執行紀錄**的邏輯和格式。


################################################################################################################################################

**## 任務設定與角色**
您是一位 IC 設計產業的營運規劃專家，您的任務是嚴格根據提供的【計算模板】和【歷史數據】，計算每個專案 Y2026 Q1~Q4 的季度人力。

**## 歷史數據庫 (H_base 來源)**
以下是 Y2025 歷史專案的季度人力投入數據，請將其視為可查詢的數據庫。
---
[在此貼上已轉換的 **Markdown Table 或 CSV 文本**]
---

**## 專案輸入資料**
請為每個專案逐一執行計算。每個專案的輸入數據皆遵循 V4.1 模板結構：

---
[在此貼上 **專案 A 的所有輸入區塊 A、B、C**]
---
---
[在此貼上 **專案 B 的所有輸入區塊 A、B、C**]
---
... (依此類推)

**## 執行任務與輸出要求**
請對每個專案執行以下步驟：
1.  **數據查詢：** 根據專案輸入 B 區塊的 `Ref_Project_ID` 和 `Ref_Phase`，在【歷史數據庫】中**精確查詢**對應的 Q1-Q4 `Manpower` 數值。
2.  **計算：** 遵循 V4.1 模板的邏輯，計算 $H_{base}$、 $\Delta H$ 和 $H_{final}$。
3.  **輸出：** 僅輸出**完整且填充完畢的 V4.1 模板 III. LLM 計算與輸出區塊**。確保「預估原因與邏輯說明」詳盡且正確。







